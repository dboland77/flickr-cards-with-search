import React, {Fragment} from "react";
import PhotoCard from "../PhotoCard/PhotoCard";

const CardGrid = () => {
  return (
    <Fragment>
      <PhotoCard />
      <PhotoCard />
      <PhotoCard />
    </Fragment>
  );
};

export default CardGrid;
